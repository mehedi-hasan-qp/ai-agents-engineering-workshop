{
  "name": "07-context",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "resume": "tsx src/index.ts --resume",
    "reset": "git checkout -- src/fixture && rm -rf .session",
    "pretest:fixture": "cd src/fixture/pokedex-api && npm install",
    "test:fixture": "cd src/fixture/pokedex-api && npm test"
  },
  "dependencies": {
    "llm-provider": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
